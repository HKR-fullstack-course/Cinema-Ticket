# Cinema-Ticket

A group project about cinema booking system

install dependencies:

```bash
npm i
```

run server, ur dir should be back-end/

```bash
npm run server
```

to run the app:

```bash
npm start
```
